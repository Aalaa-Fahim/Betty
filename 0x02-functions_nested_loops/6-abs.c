#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: the checked integer
 * Return: the absolute value of the integer
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	return (-r);
}
